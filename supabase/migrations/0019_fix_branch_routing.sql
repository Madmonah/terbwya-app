-- ============================================================================
-- إصلاح توجيه الطلب لأقرب فرع
-- الباج: `if v_branch is not null` على record في plpgsql بيرجع false لو أي
-- حقل جواه null (زي تليفون الفرع) — فالتوجيه للفرع مكنش بيحصل خالص.
-- الحل: دالة pick_nearest_pickup منفصلة بتستخدم `if found` وبترجع نقطة
-- الاستلام الصحيحة، و create_order بقت تستخدمها.
-- النسخة الكاملة متطبقة على Supabase باسم fix_branch_routing_in_create_order.
-- ============================================================================
-- (راجع دالة pick_nearest_pickup و create_order المحدثة في الداتابيز)
